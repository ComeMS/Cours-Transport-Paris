def nearbyStations(radius, positionGPSCoordinates):
  """
    finds the stations around the given position and the max distance of search
    Parameters:
    -----------
        radius: float
            the max distance where to find a station
        positionGPSCoordinates: tuple (x,y)


    Return:
    -------
    list: tuples of stations' coordinates (x,y) sorted by ascendent distance
      [(x1,y1), (x2,y2)...]  
  """
 pass

def findShortestPath(sourceStations, destStations):
    """ 
    """
    pass